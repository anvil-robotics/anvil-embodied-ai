# =============================================================================
# LIBERO validation-harness spec — Experiment 2: Anvil act-from-obs absolute pose, calibrated legacy path, Diffusion (50%)
# Historical experiment re-expressed as a bench spec (see
# packages/anvil_sim/README.md for the experiment's full story).
# Run: uv run --package anvil-sim anvil-sim-bench run configs/libero_bench/task10_ee_abs_diffusion.yaml
# =============================================================================

name: task10-ee-abs-diffusion
task_index: 10
env_suite: libero_goal
env_task_id: 8
dataset_group: abs
train:
  trainer: anvil-trainer
  action_type: ee_abs
  policy_type: diffusion
  steps: 30000
  batch_size: 16
  output_dir: model_zoo/ee-space/libero-task10-abs/diffusion
eval:
  action_type: ee_abs
  control_mode: relative
  n_episodes: 10
