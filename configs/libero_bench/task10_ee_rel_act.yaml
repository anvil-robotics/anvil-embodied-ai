# =============================================================================
# LIBERO validation-harness spec — Experiment 3: UMI-style hand-frame relative, calibrated legacy path, ACT (10%)
# Historical experiment re-expressed as a bench spec (see
# packages/anvil_sim/README.md for the experiment's full story).
# Run: uv run --package anvil-sim anvil-sim-bench run configs/libero_bench/task10_ee_rel_act.yaml
# =============================================================================

name: task10-ee-rel-act
task_index: 10
env_suite: libero_goal
env_task_id: 8
dataset_group: rel
train:
  trainer: anvil-trainer
  action_type: ee_rel
  policy_type: act
  steps: 50000
  batch_size: 16
  output_dir: model_zoo/ee-space/libero-task10-rel/act
eval:
  action_type: ee_rel
  control_mode: relative
  n_episodes: 10
