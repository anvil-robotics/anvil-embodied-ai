# goalabs family: absolute goal target G[t]=state[t]+native_delta[t] (see study.py).

name: task10-goal-abs-act
task_index: 10
dataset_group: goalabs
train:
  action_type: ee_abs
  policy_type: act
eval:
  action_type: zerocal_goal_abs
  n_episodes: 50
